namespace Snebur.UI
{
    export class ItemTemplateSelecionado extends BaseTemplate
    {
        public constructor(controlePai: BaseControle, elemento: HTMLElement)
        {
            super(controlePai, elemento);
        }

        protected override Inicializar(): void
        {
            super.Inicializar();
        }
    }
}